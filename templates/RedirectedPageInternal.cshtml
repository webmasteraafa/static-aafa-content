@inherits Umbraco.Web.Mvc.UmbracoTemplatePage<ContentModels.RedirectedPageInternal>
@using ContentModels = Umbraco.Web.PublishedContentModels;
@{
	Layout = "landingmaster.cshtml";
}
@if (Model.Content.HasProperty("umbracoInternalRedirect") && Model.Content.HasValue("umbracoInternalRedirect")) {
        var redirectUrl = Model.Content.GetPropertyValue<string>("umbracoInternalRedirect");
        Response.RedirectPermanent(redirectUrl);
}